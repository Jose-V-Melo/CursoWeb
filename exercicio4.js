function divisao_e_resto(dividendo, divisor) {
    console.log(`Resultado da divisão entre ${dividendo} e ${divisor}: ${Math.floor(dividendo/divisor)}`)
    console.log(`Resto da divisão: ${dividendo % divisor}`)
}

divisao_e_resto(4,2)